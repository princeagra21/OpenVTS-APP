# FleetStack API Coverage Gap Report (Notifications Focus)

Compared sources:
- `FleetStack-API-Reference.md`
- `X-Fleet.postman_collection.json`

Scope: Admin **Notify Users** + notification/messaging-related endpoints across Admin/Superadmin/User/Common.

## A) Summary

- Notification-scope endpoints found in Postman: **21**
- Notification-scope endpoints documented in MD (matching method+path): **21**
- Notification-scope endpoints missing in MD (Postman has them): **0**
- Notification-scope endpoints missing in Postman (MD has them): **41**

Admin Notify Users feature status (from these two sources):
- Recipient list/search: `GET /admin/users` and `GET /admin/shortusers` are present in both sources.
- Send notification endpoint (email/in-app broadcast): not explicitly present in Postman or MD for Admin.

## B) Missing in MD (Postman has it)

No notification-scope endpoints were found in Postman that are missing from MD after method+path normalization (including `:id`/numeric path normalization).

## C) Missing in Postman (MD has it) [Optional]

These notification-scope endpoints are documented in MD but not present as requests in this Postman collection snapshot:

- `GET /admin/notifications`
- `PATCH /admin/notifications/read-all`
- `PATCH /admin/notifications/:id/read`
- `GET /user/notifications`
- `PATCH /user/notifications/read-all`
- `DELETE /auth/push-token`
- `GET /admin/profile/email-subscription`
- `GET /auth/fcm-web-config`
- `GET /auth/push-tokens/me`
- `GET /superadmin/notifications`
- `GET /superadmin/profile/email-subscription`
- `GET /user/dashboard/recent-alerts`
- `GET /user/dashboard/recent-alerts/:id`
- `GET /user/notification-settings`
- `GET /user/notifications/preferences`
- `GET /user/notifications/vehicle`
- `GET /user/profile/email-subscription`
- `PATCH /admin/smtpconfig`
- `PATCH /superadmin/notifications/:id/read`
- `PATCH /superadmin/notifications/read-all`
- `PATCH /user/dashboard/recent-alerts/:id/read`
- `PATCH /user/notifications/:id/read`
- `PATCH /user/notifications/vehicle/:id/read`
- `PATCH /user/notifications/vehicle/read-all`
- `POST /admin/profile/email-subscription/subscribe`
- `POST /admin/profile/verify/email/confirm`
- `POST /admin/profile/verify/email/request`
- `POST /auth/email-test`
- `POST /auth/push-test`
- `POST /auth/push-token`
- `POST /superadmin/integrations/:id/test-fcm`
- `POST /superadmin/notifications/test-fcm-me`
- `POST /superadmin/profile/email-subscription/subscribe`
- `POST /superadmin/profile/verify/email/confirm`
- `POST /superadmin/profile/verify/email/request`
- `POST /user/notifications/test-fcm-me`
- `POST /user/profile/email-subscription/subscribe`
- `POST /user/profile/verify/email/confirm`
- `POST /user/profile/verify/email/request`
- `PUT /user/notification-settings`
- `PUT /user/notifications/preferences`

## D) Recommended Additions to `FleetStack-API-Reference.md`

- [ ] Add a dedicated **Admin Notify Users** API section with explicit send endpoint(s) and payload contracts.
- [ ] Document whether send supports single/multi channel (`email`, `in-app`, push) and required recipient identifier type (`userId` vs `email`).
- [ ] Add request/response examples for recipient search pagination/filtering for `GET /admin/users` and `GET /admin/shortusers`.
- [ ] Ensure Postman and MD are synchronized for admin/user notification inbox endpoints (list + mark-read).
- [ ] Add error contracts (`401/403/422`) for notification actions to simplify frontend handling.

---
Generated by automated comparison of method+normalized-path between MD and Postman. No credentials or tokens included.

## E) Notification Preferences (Admin)

### UI Toggles Observed (Admin app)
Source:
- `lib/modules/admin/components/notifications/notification_preferences_screen.dart`
- `lib/modules/admin/components/notifications/widget/notification_general_box.dart`
- `lib/modules/admin/components/notifications/widget/notification_vehicle_alert_box.dart`
- `lib/modules/admin/components/notifications/widget/notification_system_box.dart`
- `lib/modules/admin/components/notifications/widget/notification_dnd_box.dart`

Expected preference model from UI toggles:
- `allNotifications`
- `vehicleOffline`
- `overspeed`
- `ignition`
- `geofence`
- `sos`
- `systemAlert`
- `dndEnabled`
- optional DND schedule fields: `dndStartTime`, `dndEndTime`

Current UI behavior in app:
- Local-only toggle state.
- No backend persistence endpoint wired for these toggle preferences.

### Source Recon

#### FleetStack-API-Reference.md (relevant admin endpoints)
- `GET /admin/notifications`
- `PATCH /admin/notifications/read-all`
- `PATCH /admin/notifications/:id/read`
- `GET /admin/profile/email-subscription`
- `POST /admin/profile/email-subscription/subscribe`

Notes:
- These are inbox/read-state and email-subscription endpoints.
- No explicit Admin preferences contract found for Vehicle/System/DND toggle matrix.
- No documented Admin endpoint equivalent to user-level:
  - `GET /user/notification-settings`
  - `PUT /user/notification-settings`
  - `GET /user/notifications/preferences`
  - `PUT /user/notifications/preferences`

#### X-Fleet.postman_collection.json (notification-preferences scan)
Notification-related requests found:
- `GET /superadmin/appnotifytemplates` (folder: `Superadmin > List App Notify Templates`)
- `GET /superadmin/appnotifytemplates/:id` (folder: `Superadmin > Single App Notify Template`)
- `PATCH /superadmin/appnotifytemplates/:id` (folder: `Superadmin > Edit App Notify Template`)
  - body keys: `notifySubject`, `message`

Admin notification-preferences persistence requests found in Postman:
- None

### Comparison (Admin Notification Preferences scope)

#### Missing in MD (Postman has it)
- None for **Admin notification preferences**.

#### MD mentions but Postman missing (needs confirmation)
- No Admin notification-preferences GET/PATCH endpoints are documented in MD either.
- Therefore, Admin preference persistence remains unspecified across both sources.

### Client action needed

Admin Notification Preferences screen cannot persist to backend yet.

Minimum required capabilities to support current Admin UI toggles:
- `GET` notification preferences for current Admin account
- `PATCH` (or `PUT`) notification preferences for current Admin account
- Optional DND schedule persistence (`dndEnabled`, `dndStartTime`, `dndEndTime`)
- Device push token registration lifecycle for mobile/web push (register/update/remove token)

Until these endpoints are confirmed and documented, Admin Notification Preferences should remain local-only.
