// Placeholder for vehicle command panel widget
import 'package:flutter/material.dart';

class VehicleCommandPanel extends StatelessWidget {
  const VehicleCommandPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(child: Text('Vehicle Command Panel'));
  }
}