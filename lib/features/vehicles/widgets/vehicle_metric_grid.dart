// Placeholder for vehicle metric grid widget
import 'package:flutter/material.dart';

class VehicleMetricGrid extends StatelessWidget {
  const VehicleMetricGrid({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(child: Text('Vehicle Metric Grid'));
  }
}