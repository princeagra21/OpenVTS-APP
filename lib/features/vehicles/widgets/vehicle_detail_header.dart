// Placeholder for vehicle detail header widget
import 'package:flutter/material.dart';

class VehicleDetailHeader extends StatelessWidget {
  const VehicleDetailHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(child: Text('Vehicle Detail Header'));
  }
}