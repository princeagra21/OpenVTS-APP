// Placeholder for API config save bar widget
import 'package:flutter/material.dart';

class ApiConfigSaveBar extends StatelessWidget {
  const ApiConfigSaveBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Row(children: [Text('Save Bar')]);
  }
}