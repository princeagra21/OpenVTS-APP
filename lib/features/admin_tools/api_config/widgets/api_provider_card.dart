// Placeholder for API provider card widget
import 'package:flutter/material.dart';

class ApiProviderCard extends StatelessWidget {
  const ApiProviderCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(child: Text('API Provider Card'));
  }
}