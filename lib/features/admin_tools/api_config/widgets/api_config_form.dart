// Placeholder for API config form widget
import 'package:flutter/material.dart';

class ApiConfigForm extends StatelessWidget {
  const ApiConfigForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(child: Text('API Config Form'));
  }
}