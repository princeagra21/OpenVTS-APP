// Placeholder for API test connection card widget
import 'package:flutter/material.dart';

class ApiTestConnectionCard extends StatelessWidget {
  const ApiTestConnectionCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(child: Text('Test Connection'));
  }
}