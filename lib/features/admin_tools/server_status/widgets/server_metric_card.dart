// Placeholder for server metric card widget
import 'package:flutter/material.dart';

class ServerMetricCard extends StatelessWidget {
  const ServerMetricCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(child: Text('Server Metric Card'));
  }
}