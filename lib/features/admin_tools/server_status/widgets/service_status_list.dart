// Placeholder for service status list widget
import 'package:flutter/material.dart';

class ServiceStatusList extends StatelessWidget {
  const ServiceStatusList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(child: Text('Service Status List'));
  }
}