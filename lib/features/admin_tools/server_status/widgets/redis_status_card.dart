// Placeholder for Redis status card widget
import 'package:flutter/material.dart';

class RedisStatusCard extends StatelessWidget {
  const RedisStatusCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(child: Text('Redis Status Card'));
  }
}