// Placeholder for server log panel widget
import 'package:flutter/material.dart';

class ServerLogPanel extends StatelessWidget {
  const ServerLogPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(child: Text('Server Log Panel'));
  }
}